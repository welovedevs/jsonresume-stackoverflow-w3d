{{#if resume.languages.length}}
<section class="section">
  <header>
    <h2 class='section-title'>Langages</h2>
  </header>
  <section id="languages">
    {{#each resume.languages}}
    <div class="display">
      {{#if language}}
      <h3 class="language">
        {{language}}
      </h3>
      {{/if}}
      <div class="item">
        {{#if value}}
        <div class="level fluency {{getLevel value}}">
          <em>{{value}} %</em>
          <div class="bar"></div>
        </div>
        {{/if}}
      </div>
    </div>
    {{/each}}
  </section>
</section>
{{/if}}
